# DigitalOcean — IBKR stack (deploy via scripts/deploy-ibkr-do.ps1 or GitHub Actions)
#
#   .do/ibkr-app.yaml         → IBKR_API_URL + WebSocket base
#   .do/monte-carlo-app.yaml  → MONTE_CARLO_API_URL
#
# Vercel hosts Next.js only. IB Gateway runs on a DO Droplet (see infra/digitalocean/).

name: general-exchange-ibkr
region: nyc

# Deploy components separately — see ibkr-app.yaml and monte-carlo-app.yaml
