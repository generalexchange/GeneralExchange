# Option A — three DO apps (one service each; avoids ingress conflicts)
# Deploy all: .\scripts\deploy-backend-do.ps1
#
#   .do/ws-app.yaml           → NEXT_PUBLIC_WS_URL=wss://<url>/ws
#   .do/go-api-app.yaml       → GO_API_URL=https://<url>
#   .do/monte-carlo-app.yaml  → MONTE_CARLO_API_URL=https://<url>
#
# Vercel hosts Next.js only.

name: general-exchange-ws
region: nyc

# See ws-app.yaml, go-api-app.yaml, monte-carlo-app.yaml — deploy separately.
